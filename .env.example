# Sniff CLI Environment Configuration
# Copy to .env and fill in your values

# =============================================================================
# OPTIONAL
# =============================================================================

# Proxy URL for OAuth and webhooks (default: https://api.sniff.to)
# Only set this if you're self-hosting your own proxy
# SNIFF_PROXY_URL=https://your-proxy.workers.dev

# Local server port (default: 3847)
# SNIFF_PORT=3847

# Linear webhook secret for signature verification
# Must match WEBHOOK_SECRET in your proxy
# LINEAR_WEBHOOK_SECRET=
