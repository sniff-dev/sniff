# Cloudflare Worker Environment Variables
# Copy to .env for local development with `wrangler dev`
#
# For production: use `wrangler secret put <NAME>` instead

# =============================================================================
# REQUIRED - Linear OAuth App Credentials
# =============================================================================
# Create at: Linear Settings > API > OAuth Applications

LINEAR_CLIENT_ID=your-oauth-client-id
LINEAR_CLIENT_SECRET=your-oauth-client-secret

# =============================================================================
# OPTIONAL
# =============================================================================

# Webhook signature verification (recommended for production)
# Get this when creating a webhook in Linear Settings > API > Webhooks
# Must match LINEAR_WEBHOOK_SECRET in CLI's .env
# WEBHOOK_SECRET=

# Your tunnel URL for local development
# The proxy forwards webhooks here
# TUNNEL_URL=https://your-tunnel.ngrok-free.app
