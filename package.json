{
  "name": "sniff",
  "description": "Local-first AI agents for Linear",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/caiopizzol/sniff.git"
  },
  "bugs": {
    "url": "https://github.com/caiopizzol/sniff/issues"
  },
  "homepage": "https://github.com/caiopizzol/sniff#readme",
  "scripts": {
    "dev:cli": "bun run --filter @sniff-dev/cli dev",
    "dev:proxy": "bun run --filter @sniff-dev/proxy dev",
    "dev:web": "bun run --filter @sniff-dev/web dev",
    "dev:docs": "bun run --filter @sniff-dev/docs dev",
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "lint": "bunx @biomejs/biome check .",
    "lint:fix": "bunx @biomejs/biome check --write .",
    "format": "bunx @biomejs/biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/bun": "latest",
    "semantic-release": "^24.2.5",
    "@semantic-release/exec": "^7.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.3",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}